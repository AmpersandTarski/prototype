# SonarCloud automatic-analysis configuration.
#
# The Docusaurus sidebar is a config file whose entries are necessarily
# identical in shape ({ label, type, id }), so every added entry trips the
# copy-paste detector on "new duplicated lines". Exclude it from duplication
# analysis (it stays included in all other analyses).
sonar.cpd.exclusions=docs/sidebar.js
