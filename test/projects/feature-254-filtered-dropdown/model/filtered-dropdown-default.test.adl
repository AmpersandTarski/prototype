CONTEXT FilteredDropdownsTestsDefault

REPRESENT DefaultName TYPE ALPHANUMERIC
REPRESENT DefaultStatus TYPE ALPHANUMERIC

RELATION defaultName[Employee*DefaultName] [UNI]
RELATION defaultStatus[Employee*DefaultStatus] [UNI]
RELATION projectMemberDefault[Project*Employee]

POPULATION defaultName[Employee*DefaultName] CONTAINS
  [ ("m1", "Jan Jansen")
  , ("m2", "Piet Peters")
  , ("m3", "Marie Bakker")
  , ("m4", "Klaas Klaasen")
  , ("m5", "Anna Annasson")
  ]

POPULATION defaultStatus[Employee*DefaultStatus] CONTAINS
  [ ("m1", "eligible")
  , ("m2", "on leave")
  , ("m4", "eligible")
  , ("m3", "fired")
  , ("m5", "eligible")
  ]

POPULATION projectMemberDefault[Project*Employee] CONTAINS
  [ ("project 1", "m1")
  , ("project 2", "m1")
  , ("project 2", "m2")
  ]

-- Desired filtering: eligible employees only
RELATION defaultEligibleEmployees[Employee] [PROP]
ENFORCE defaultEligibleEmployees := defaultStatus;"eligible";defaultStatus~ /\ I
-- As a result, defaultEligibleEmployees contains [ ("m1","m1"), ("m4","m4"), ("m5","m5")]

-- I want the user to pick project members in the relation `projectMemberDefault`, using `defaultEligibleEmployees` to select from.

INTERFACE DropdownsDefault : "_SESSION"[SESSION]; V[SESSION*Project] cRUd BOX<FORM>
  [ "Project ID" : I
  , "Section Header" : TXT "ALL 8 VARIATIONS with the R permission"

  , "Assign an employee (cRud)" : projectMemberDefault cRud BOX<FILTEREDDROPDOWN>
  [ select : defaultEligibleEmployees ]  
  , "Assign an employee (cRUd)" : projectMemberDefault cRUd BOX<FILTEREDDROPDOWN>
  [ select : defaultEligibleEmployees ]  
  , "Assign an employee (cRuD)" : projectMemberDefault cRuD BOX<FILTEREDDROPDOWN>
  [ select : defaultEligibleEmployees ]  
  , "Assign an employee (cRUD)" : projectMemberDefault cRUD BOX<FILTEREDDROPDOWN>
  [ select : defaultEligibleEmployees ]  
  , "Assign an employee (CRud)" : projectMemberDefault CRud BOX<FILTEREDDROPDOWN>
  [ select : defaultEligibleEmployees ]  
  , "Assign an employee (CRUd)" : projectMemberDefault CRUd BOX<FILTEREDDROPDOWN>
  [ select : defaultEligibleEmployees ]  
  , "Assign an employee (CRuD)" : projectMemberDefault CRuD BOX<FILTEREDDROPDOWN>
  [ select : defaultEligibleEmployees ]  
  , "Assign an employee (CRUD)" : projectMemberDefault CRUD BOX<FILTEREDDROPDOWN>
  [ select : defaultEligibleEmployees ]  

  , "Section Header 2" : TXT "ALL 8 VARIATIONS without the R permission"
  , "Note" : TXT "(These result in: object is not readable, but also generate red box)"
  -- , "Assign an employee (crud)" : projectMemberDefault crud BOX<FILTEREDDROPDOWN>
  -- [ select : eligibleEmployees ]  
  -- , "Assign an employee (crUd)" : projectMemberDefault crUd BOX<FILTEREDDROPDOWN>
  -- [ select : eligibleEmployees ]  
  -- , "Assign an employee (cruD)" : projectMemberDefault cruD BOX<FILTEREDDROPDOWN>
  -- [ select : eligibleEmployees ]  
  -- , "Assign an employee (crUD)" : projectMemberDefault crUD BOX<FILTEREDDROPDOWN>
  -- [ select : eligibleEmployees ]  
  -- , "Assign an employee (Crud)" : projectMemberDefault Crud BOX<FILTEREDDROPDOWN>
  -- [ select : eligibleEmployees ]  
  -- , "Assign an employee (CrUd)" : projectMemberDefault CrUd BOX<FILTEREDDROPDOWN>
  -- [ select : eligibleEmployees ]  
  -- , "Assign an employee (CruD)" : projectMemberDefault CruD BOX<FILTEREDDROPDOWN>
  -- [ select : eligibleEmployees ]  
  -- , "Assign an employee (CrUD)" : projectMemberDefault CrUD BOX<FILTEREDDROPDOWN>
  -- [ select : eligibleEmployees ]  
  ]
    

ENDCONTEXT
