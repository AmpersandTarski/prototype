# Entry model of this project (default: model/main.adl)
entry=model/app.adl
# Reported as "known red" instead of FAIL until the compiler lays broad tables out differently:
known_fail="#415: the IfcRoot broad table needs 183 VARCHAR(255) columns (116 concept-identity, 21 reference, 46 value), far over the MySQL row-size limit of 65535 bytes"
